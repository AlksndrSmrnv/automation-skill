<!--
ШАБЛОН ОБЗОРНОГО СКИЛЛА СТИЛЯ ПРОЕКТА (Этап 4).

Создаётся ОДИН раз как <output>/autotest-house-style/SKILL.md.
Это первый скилл, который грузит будущий оркестратор: он описывает «как вообще устроен тест в
этом проекте», чтобы остальные скиллы-возможности вставали в общий каркас.

Заполни плейсхолдеры <...> данными из карты проекта (Этап 1). Удали HTML-комментарии.
Содержимое на русском; код/символы/импорты — как в проекте.
-->

=== НАЧАЛО SKILL.md ===

---
name: autotest-house-style
description: >-
  Описывает соглашения и каркас автотестов проекта (Kotlin/JUnit5/Allure): базовый тест-класс,
  получение зависимостей, конфиг и окружение, Allure-разметку, библиотеку ассертов, тестовые
  данные и жизненный цикл. Используй ВСЕГДА перед написанием или правкой автотеста в этом
  проекте, чтобы тест соответствовал принятому стилю. Триггеры: «напиши автотест», «добавь тест»,
  «как устроены тесты у нас», house style, test conventions.
---

# Стиль автотестов проекта

Загружай этот скилл первым перед написанием теста. Конкретные действия (Kafka, REST, K8s и т.д.)
описаны в отдельных скиллах-возможностях — здесь общий каркас, в который они встраиваются.

## Стек и структура
- **Стек:** Kotlin <версия> / Java 21 / JUnit5 / Allure <версия>; сборка — <Gradle/Maven>.
- **Модули тестов:** <перечисли модули и где лежат тесты, корневой пакет>.
- **Слой, на котором пишутся тесты:** <напр. `steps` — тесты вызывают `*Steps` методы с @Step>.

## Базовый тест-класс и жизненный цикл
- **Базовый класс:** `<BaseTest>` ([<file>:<line>](<путь>:<line>)) — <что даёт>.
- **Расширения:** <@ExtendWith / кастомные Extension>.
- **Setup:** <что в @BeforeAll/@BeforeEach: контекст, логин, подготовка данных>.
- **Cleanup:** <что в @AfterEach/@AfterAll: очистка данных, закрытие ресурсов>.
- **Группировка:** <@Nested, @Tag, @DisplayName, параметризация>.

## Получение зависимостей (DI)
<Как тест получает степы/клиенты: Spring @Autowired / ручное создание / object. Покажи канонично.>
```kotlin
<минимальный пример: объявление зависимости + её использование>
```

## Конфигурация и окружение
- **Конфиг:** <файлы, профили, как читается>.
- **Окружение:** <Testcontainers/стенды; что поднимается локально, что внешнее, по подсистемам>.

## Allure-разметка
- **Класс:** `<@Epic/@Feature/@Story/@DisplayName — как принято>`.
- **Шаги:** `<@Step на методах степов и/или Allure.step("...") { } в теле>`.
- **Вложения:** `<Allure.addAttachment/@Attachment — когда применяется>`.

## Ассерты и асинхронность
- **Ассерты:** <библиотека и идиома: AssertJ `assertThat(...)` / Kotest `shouldBe` / JUnit>.
- **Async:** <Awaitility/поллинг/ретраи — как принято ждать результата>.

## Тестовые данные
<Билдеры/фабрики/генераторы; как создаются и как чистятся.>

## Скелет типичного теста
<!-- Снять с реального теста и обобщить: показать сборку теста из шагов end-to-end. -->
```kotlin
<полный, но минимальный пример теста: аннотации класса → DI → @Test → arrange/act/assert через
 степы с Allure-шагами → cleanup>
```

## Карта скиллов-возможностей
Действия описаны в отдельных скиллах (заполнится после генерации каталога):
- [[<подсистема-действие>]] — <короткое описание>.
- <... остальные ...>

## Источники
- Базовый класс: [<file>:<line>](<путь>:<line>)
- Образцовые тесты: [<file>:<line>](<путь>:<line>), [<file>:<line>](<путь>:<line>)
