<!--
ШАБЛОН СКИЛЛА-ВОЗМОЖНОСТИ (Этап 4).

Как пользоваться:
- Скопируй всё, что НИЖЕ строки «=== НАЧАЛО SKILL.md ===», в файл
  <output>/<имя-скилла>/SKILL.md и заполни плейсхолдеры <...> данными из реального кода.
- Удали HTML-комментарии-подсказки (вроде этого) — в финальном скилле их быть не должно.
- Все плейсхолдеры обязательны; если данных нет — см. «Если данных не хватило» в quality-checklist.md.
- Содержимое на русском; код, имена символов и импорты — как в проекте (английские).
- Каждый file:line должен существовать. Сниппет — снят с реального теста, не псевдокод.
-->

=== НАЧАЛО SKILL.md ===

---
name: <подсистема>-<действие>            # kebab-case по маске ^[\p{L}\p{N}_:.-]+$, совпадает с именем папки
description: >-
  <Одно-два предложения: что делает возможность + когда её применять в тесте>. Используй, когда
  шаг теста требует <...>. Триггеры: <русские фразы, как это звучит в шагах теста>, <ключевые
  EN-слова: имена технологии/метода>.
                                          # Qwen — model-invoked: подключает скилл по description.
                                          # Делай его богатым на естественные формулировки шага теста:
                                          # что делает + когда применять + RU/EN-триггеры + имена технологий.
# paths: ['**/src/test/**/*.kt']         # ОПЦИОНАЛЬНО: включать скилл только при работе с тест-файлами
---

# <Человекочитаемое название возможности>

## Когда использовать
<В каких шагах теста это нужно. 1–3 пункта. Когда НЕ использовать / что взять вместо — если есть
близкая возможность, укажи её имя `другой-скилл`.>

## Что делает
<Одно предложение по сути действия.>

## API / точка входа
- **Класс:** `<FQN или короткое имя>`  ([<file>:<line>](<относительный путь>:<line>))
- **Метод:** `<сигнатура: fun name(args): ReturnType>`
- **Импорт:** `import <реальный.пакет.Класс>`
- **Как получить объект:** <DI или создание — точно как в проекте, напр. `@Autowired lateinit var steps: KafkaSteps`
  или `val steps = KafkaSteps(config)`>

## Минимальный пример в тесте
<!-- Снято с реального теста <file>:<line> и минимально адаптировано. Должно компилироваться в стиле проекта
     и включать Allure-шаг так, как принято в проекте. Проверка результата — ТОЛЬКО если она естественна
     для этого действия (см. ниже). Для act/setup-атомов (отправка, seed, restart, cleanup) проверки в
     примере может не быть. НИКАКИХ секретов/реальных хостов/токенов — только плейсхолдеры. -->
```kotlin
<рабочий Kotlin-сниппет: получение объекта → вызов с реальными аргументами → @Step/Allure.step
 (+ проверка результата, если она естественна для действия)>
```

## Параметры / нужный конфиг и env
| Параметр | Тип | Обязателен | Описание / значение по умолчанию |
|----------|-----|------------|----------------------------------|
| `<arg>` | `<Type>` | да/нет | <что это, дефолт> |

**Конфиг/окружение:** <что должно быть настроено, чтобы вызов работал — properties/env, поднятый
контейнер или доступ к стенду; откуда берётся (Этап 1 §6). Указывай ключи конфига/имена env
(`kafka.bootstrap-servers`, `${DB_PASSWORD}`), а НЕ реальные значения, хосты, токены, пароли.>

## Возвращает и как проверять
<!-- Если действие само проверяет результат (вернуло данные/статус) — опиши проверку идиомой проекта.
     Если это act/setup-атом без осмысленного возврата (отправка в Kafka, seed, restart, cleanup) —
     НЕ выдумывай assertion: укажи наблюдаемую пост-проверку и/или связанный скилл для проверки. -->
- **Возвращает:** `<тип/структура или «ничего значимого»>` — <что внутри>.
- **Проверка:** <если возврат есть — идиома ассертов проекта, напр. `assertThat(result.status).isEqualTo(200)`
  или `result shouldBe ...`. Если возврата нет — наблюдаемая пост-проверка (например, «дождись записи
  через `kafka-consume-await`», «проверь rollout через `k8s-wait-ready`») со ссылкой на связанный скилл.>

## Подводные камни
<Реальное поведение, не общие слова: таймауты/ретраи, асинхронность (нужен ли await/поллинг),
идемпотентность, обязательная очистка после теста, флакость, ограничения окружения.>

## Связанные скиллы
<!-- В Qwen нет wiki-ссылок: указывай имена скиллов, их можно вызвать через /skills <имя>. -->
- `autotest-house-style` — общий стиль и устройство теста.
- `<родственный-скилл>` — <чем полезен рядом, напр. отправил → дождись>.

## Источники
<Файлы реального кода, на которых основан скилл:>
- Точка входа: [<file>:<line>](<путь>:<line>)
- Пример использования: [<file>:<line>](<путь>:<line>)
